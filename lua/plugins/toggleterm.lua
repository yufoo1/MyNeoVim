require("toggleterm").setup{
    open_mapping = [[<c-\>]],
    direction = 'float',
    float_opts = {
    --     border = 'single' | 'double' | 'shadow' | 'curved' | ... other options supported by win open
    border = 'curved',
    },
}
